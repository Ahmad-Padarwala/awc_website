module.exports = {
    siteUrl: 'https://awcindia.in',
    generateRobotsTxt: true, 
    exclude: ['/admin/*'], // Exclude all routes starting with /admin/
    changefreq: null, // Disable changefreq tag
    priority: null, // Disable priority tag
    lastmod: null // Disable lastmod tag
  }